let initialState = 0


function Premium(state = initialState,action){

    if(action.type === "premium"){

        return initialState = initialState

    }

    return state
    
}

export default Premium;